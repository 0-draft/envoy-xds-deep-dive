# A small kind cluster for the pod-to-pod lab: one control-plane node plus one
# worker so pods actually get scheduled onto a "real" node.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: envoy-xds
nodes:
  - role: control-plane
  - role: worker
